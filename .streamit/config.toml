GROQ_API_KEY="gsk_placeholder_replace_this_locally"

[server]
# Instructs Streamlit to dynamically watch and include the root module path
clientPaths = true

[python]
# Forces the execution context to remain relative to the project root directory
runFromRoot = true